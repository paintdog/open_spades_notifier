A small terminal tool that checks the [OpenSpades](https://github.com/yvt/openspades) server list for a specific server and maps.

To start the script, run the following command in your terminal:

``` bash
python open_spades_notifier.py
```
